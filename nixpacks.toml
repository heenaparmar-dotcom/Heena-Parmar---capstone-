# Playwright's headless Chromium needs these OS-level shared libraries
# (libglib-2.0.so.0 etc.) at runtime. Railway's Nixpacks builder can install
# real apt packages via aptPkgs — this is more reliable than relying on a
# manually-set "--with-deps" build command in the dashboard, which uses
# apt-get internally and doesn't consistently work under Nixpacks.
[phases.setup]
aptPkgs = [
  "libglib2.0-0", "libnss3", "libnspr4", "libatk1.0-0", "libatk-bridge2.0-0",
  "libcups2", "libdrm2", "libdbus-1-3", "libxcb1", "libxkbcommon0",
  "libx11-6", "libxcomposite1", "libxdamage1", "libxext6", "libxfixes3",
  "libxrandr2", "libgbm1", "libpango-1.0-0", "libcairo2", "libasound2"
]

[phases.build]
cmds = ["npm install", "npx playwright install chromium"]
